---
type: Context Note
title: 'PR #133 adversarial QA — 9ac537b'
actor: codex-qa-9ac537b
timestamp: '2026-07-21T13:59:56.568Z'
---
# Summary

Independent adversarial QA completed against exact repair commit `9ac537be28bb627656f5328e06929f496ad80aac` (parent `fc9474c7b7d1cfa64002f4b1ccc8c66f0dc38e1f`).

Ultimate goal: agentstate-lite is plain-text, local-first, conflict-safe shared memory for humans and agents.

Proximate QA goal: try to falsify that the reviewed board fix completely preserves task lifecycle and safely renders valid bundle IDs; this protects trustworthy state visualization.

Progress: complete.

# Verdict

PASS, high confidence. No QA findings.

# Survived attacks

- A canceled Task with a Project, high priority, assignee, and unique search title stayed visible when Project, priority, and search filters were applied together. The card remained in the fifth lifecycle column and retained its reachable Reopen action.
- Proposing the already-current priority returned `unchanged`; canceling a prepared Reopen returned `cancelled`. Both rendered with exact neutral class `toast show`, without the error class.
- Applying Reopen persisted `status: todo` and `actor: e2e/human`, and the live View moved the Task to the first column.
- After a Start proposal reached the prepared trusted-shell dialog, an external write changed the Task priority. Applying the stale proposal returned `conflict`, rendered `Change conflict.` with exact class `toast show error`, and did not clobber the external state: status remained `todo` and priority remained `medium`. This directly exercised hard CAS after confirmation preparation.
- The committed `constructor` Task rendered its dependency count. A second fresh bundle replaced that root Task with root Project id `constructor`, then added root Task ids `__proto__` and `toString`, each participating in Project and dependency edges. Seven cards and two Projects rendered, both dependency counts and Project chips resolved, and filtering by Project id `constructor` returned exactly the two special-id Tasks.
- Static scope audit found the repair confined to the board HTML and its E2E seed/journey; existing escaping, bridge policy, and persistence boundaries were unchanged.

# Evidence

- Detached worktree HEAD: `9ac537be28bb627656f5328e06929f496ad80aac`; worktree clean before and after probes.
- Fresh `npm ci`: exit 0.
- Root `npm run build`: exit 0.
- Temporary QA-only Playwright spec, real built CLI + embedded UI, Chromium: `2 passed (4.4s)`.
- The initial lifecycle probe attempted the external mutation before explicitly awaiting the prepared confirmation dialog, so the proposal legitimately captured the newer version and committed; the special-id attack still passed. This was a QA timing error. The corrected probe awaited the visible, enabled confirmation before racing the document.
- A sandboxed retry then failed before test execution because Chromium could not register its macOS Mach-port rendezvous (`Permission denied`). Running the same corrected command with the required execution permission produced the authoritative 2/2 pass.
- Temporary probe file was removed; `git status --short` is empty and `git diff --check` passes.

# Conclusion

The exact reviewed SHA preserves the full five-state Task lifecycle, safely indexes valid prototype-key concept IDs, classifies neutral and failure outcomes correctly, and retains hard-CAS write protection. It is ready for branch push and CI.
