---
type: Context Note
title: npm quickstart adversarial QA at 8c9360b
actor: codex-npm-quickstart-qa
timestamp: '2026-08-07T18:20:35.307Z'
---
# Summary

Adversarial QA APPROVES exact clean SHA 8c9360bb098f33302337d489a54dd0fcb0e16f24 with zero findings after a literal isolated installed-artifact journey, retained package-safety proof, full repository gate, clean-tree check, and residual-process audit.

# Verdict

APPROVE exact clean SHA 8c9360bb098f33302337d489a54dd0fcb0e16f24 against base 531c9df8ac7299f662d87862d270c7eb63f7dfab. No blocker, regression, scope drift, or residual-process finding. Any subsequent code change restarts Review and QA.

# Goals

Ultimate goal: make agentstate-lite the shared, versioned, conflict-safe Markdown memory that a human and agent fleet can install and use without founder intervention.

Proximate goal: adversarially prove that the exact installed npm candidate takes a normal non-empty project from orientation to a conventional bundle and honestly attributed productive Task while preserving the merged package-safety barriers. This serves the ultimate goal by verifying first value without founder explanation or destructive project writes.

# Exact scope and artifact

- Detached QA worktree remained at exact SHA 8c9360bb098f33302337d489a54dd0fcb0e16f24.
- Base-to-head diff remained exactly the approved nine files: both READMEs; home and recipes source/tests; update-orientation golden; package verifier and static verifier test.
- Worktree was clean before and after QA; git diff --check passed.
- Built and packed the exact source into holaxis-aslite-0.1.0-pre.3.tgz, 30 files, shasum 07e4c5fd82d78799aedf6125eeb2d2c3914d2fec. Installed that tarball exactly once into an isolated global prefix using an isolated npm cache and offline mode. Both installed bins resolve to the packaged artifact.

# Installed first-value attack

- The test project was genuinely non-empty before discovery: an unrelated root file and nested src/unrelated.json were present.
- Installed home and recipes JSON inspection produced zero project writes; full file-hash manifests before and after were identical.
- Bundle-free home targeted the conventional child .agentstate-lite. Bundle-free recipe rows exposed create_bundle commands targeting .agentstate-lite.
- Executed the emitted work-tracking command unchanged through the shell: aslite init --create-only --recipe work-tracking --dir .agentstate-lite. It created only the conventional child and preserved unrelated content.
- Re-executed the exact same emitted string. It exited 5 with ALREADY_EXISTS at preflight and residual_created_directories empty. Whole-project tar snapshots before and after retry were byte-identical with SHA-256 7d27edc48fc4337b87008059a8953ba86f4bc77554a622fd6f76821f810b169d.
- Existing local recipe projections covered applied false and true in the same real bundle and were add-only; no create_bundle key or create help remained. A live installed loopback reference server proved remote false/true rows were also add-only and carried only explicit --remote add commands.
- Created tasks/qa-first-task with actual actor codex-npm-quickstart-qa. doc read retained the actor; list returned the Task; home surfaced it in recent rows; status reported kind_warnings 0.

# Public contract and retained safety

- Both public READMEs use unqualified npm install -g @holaxis/aslite, contain no default @next install, retain canonical --scope user, and label quickstart-agent as an advisory example to replace with the actual agent identity.
- README prose keeps agents, rather than users, responsible for organizing, typing, linking, and updating bundle content from intent or source material.
- The exact package verifier passed with source commit 8c9360bb098f33302337d489a54dd0fcb0e16f24 and dirty=false, 30-file allowlist, zero runtime dependencies, both command names, and offline workflow.
- Static and dynamic package proof retained PR 212 source/artifact identity, whole-tree no-delete/refusal snapshots, retained candidate semantics, nested-publication exclusion, and the externally instrumented real production-lock holder/contender barrier on the same lock path with EEXIST, no pre-release publication, and loser exit 5.

# Test evidence

- npm ci: pass.
- npm run build: pass.
- npm run typecheck: pass.
- Focused home + recipes + update-orientation suites: 102/102 pass.
- npm run test:scripts: 129/129 pass, including README contract and production-lock structure tests.
- npm run verify:npm-package: pass on the exact clean source identity.
- Full unrestricted npm run check: exit 0, including the complete workspace suites, scripts 129/129, exact installed-package proof, skill drift gate, MCP browser 15/15, and UI Playwright 19/19.
- Final read-only process audit found no QA worktree, isolated installed prefix/project, Playwright, or Chromium survivors.

# Environmental observations

- The first npm pack attempt was blocked before artifact creation by root-owned entries in the user npm cache. QA moved to a fresh isolated cache instead of modifying user state; pack and install then passed.
- A direct node invocation of the verifier static test intentionally failed its npm_execpath precondition. Running the owning npm run test:scripts command passed all 129 tests.
- Sandbox loopback bind returned EPERM; the authorized unrestricted reference-server probe passed and was terminated cleanly.

# Handoff

This note is intentionally unsynced for the orchestrator to share together with the task update. The exact SHA is ready for the orchestrator push-with-lease, PR creation, and hosted Node 20, 22, and 26 gates. Do not merge from this QA role.
