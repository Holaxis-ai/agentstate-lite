---
type: Context Note
title: T3.5 H2-H5 boundary R2 acceptance — PASS
actor: codex-t35-h2-h5-acceptance-r2
timestamp: '2026-08-04T18:21:31.172Z'
---
# Summary

Status: **complete**.

Verdict: **PASS**.

Confidence: **0.97**.

`builder_task_eligible: true`

Exact R2 is complete and mechanically implementable for the next static builder-authoring stage. It closes the R1 fence contradiction and all four R1 skeptic blockers without weakening failure containment or promoting retained v5 evidence into a production oracle. The two scope-sensitive judgments are explicit and binding: I accept top-level `-N` as a pinned option-level architecture premise for the exact tmux binary, and I accept H4 only as a controller-owned declared-principal topology/ordering counterexample—not as evidence about pane-owned detachers, arbitrary descendants, or real Claude containment.

## Isolation and exact inputs

I acted only as the fresh R2 product/acceptance reviewer. I read both frozen R1 FAILs because they are mandatory R2 inputs. I did not inspect any current R2 skeptic output or communicate with the skeptic. Review was static and read-only apart from my uniquely owned task/note writes: no host probe or tmux process/action, Claude/API/auth/network action, test, repository code or Plan mutation, parent-task/shared-handoff mutation, or board sync.

Exact bundle inputs:

- decision `decisions/precompact-v3-t35-reuse-v5-no-autostart@sha256:db1509fc65afdbffe09ef9e4fae936bd86e94ed7a1055a1677afd78e3218665d`
- R2 boundary `designs/precompact-v3-t35-h2-h5-host-probe-boundary@sha256:1847717b5456a3cea3325cab947543c91cc0c6cb00403d4b99d00a5971c56b51`
- R1 acceptance FAIL `context-notes/precompact-v3-t35-h2-h5-boundary-acceptance@sha256:9ab4554b694a5f573fc53d8b22e816e834e8c3999a11136684324ed5abb768f7`
- R1 skeptic FAIL `context-notes/precompact-v3-t35-h2-h5-boundary-skeptic@sha256:22c47a846c9dbe7d22742c73babc043469b537caf62c035c2de81682f8b16717`
- retained Research `research/precompact-v3-t35-launch-reaper-host-probe@sha256:2f910d13a66e4a95f886dccf2bfbbb9be9576c17be51cb7e922bcd0a9a18d3cf`
- retained audit `context-notes/precompact-v3-t35-host-probe-evidence-audit@sha256:f03b67e1e399631d9f63bb4a0f6afd4edbbdc93bac255a35b88490c626c57a01`

Exact retained local inputs were present and hash-matched during static review:

- `launch-probe.mjs@sha256:c78ee01ee720c6c5e9b3a7fc943233d601c91634b12908e5705cebc420eb2448`
- `evidence.json@sha256:063280001ce146eec5f3a8f6ba83b5edec45076199ef7df6115726a7215424d9`
- `summary.json@sha256:39058982be79a6795a3091d7cc6e21b525a02019d2d4570f7eeba64f1a9f39cc`
- `/opt/homebrew/Cellar/tmux/3.6b/bin/tmux@sha256:e0c40b227b8d7283c4ace45056c70cf0fe371bd00b62e52b6e14fadae5d69f70`
- `/opt/homebrew/Cellar/tmux/3.6b/share/man/man1/tmux.1@sha256:6903664073d73ae13d764216917932a4d5a212ce6b8e836297a58b3bf58c78ca`

Task claim: CAS from `sha256:3e464f6ae16949e8b960be78a3c2877371756a059e94b4163dcc40bc3a599f0b` to in-progress `sha256:abfa9c13f98e5dab41075028b721dda07d261286d24de810a2d0db03f908494a` as `codex-t35-h2-h5-acceptance-r2`.

## Explicit option-level `-N` adjudication

**Accepted as an architecture premise, not as expanded empirical evidence.** The exact pinned installed manpage defines top-level `-N` as preventing server start even when the selected command would normally start one. The exact v5 source/evidence empirically corroborate absent-socket `-N new-session` and `-N kill-server` failure without socket creation, plus live `list-sessions` and `display-message` behavior. Together those inputs justify applying the documented top-level option semantics to R2's three immutable vector families on this exact binary.

This acceptance does not claim that v5 empirically ran every R2 vector against absence. R2 labels the cross-command step as a reviewed inference, verifies the exact binary/manpage hashes before spawn, uses `-N` on every client, forbids deliberately absent targets and arbitrary commands, and makes any sampled-liveness race an overall primary FAIL followed by passive generation/process containment. Execution is not allowed to re-adjudicate this premise. If a later artifact describes the inference as fresh proof, command-tail-specific empirical parity, or production acceptance, that artifact must fail review.

## Explicit H4 scope adjudication

**Accepted only for controller-owned declared principals.** The pane writes a request but performs no spawn. The controller alone creates each marked companion with `detached:true` and retains the direct handle from the creation syscall; therefore missing, malformed, or stalled companion self-recording cannot create the R1 unowned-process trace. The abort latch rejects later requests before spawn, and original-handle/group containment remains available independent of the record's success.

H4 remains useful as a physical teardown-order counterexample: server-first removal can leave a separately grouped, directly owned companion live, while draining that declared principal before the final server action yields the intended terminal order. It does **not** prove discovery or containment of a pane-owned detacher, unmarked escape, hook/sub-agent tree, or real Claude descendant. A replacement Plan may consume H4 only if its lifecycle authority itself launches and retains the relevant principal. Any generalization beyond that scope requires separate design/evidence and must fail review.

## R1 closure matrix

- **Abort/final-action contradiction:** closed. `abort-latch.json` stops future creators; server-B kill is the final authorized tmux action; `action-fence.json` is created only after its close/both-EOF/identity/server-absence receipts; no tmux process follows the action fence.
- **Target-disappearance race:** closed within selected option 1. Sampled exact-live remains an honest precondition, while accepted top-level `-N` makes a disappearance-before-contact outcome non-creating; the action/result case is still FAIL and cannot become primary PASS.
- **H2 controller conditioning:** closed. Both observation schedules are sealed/read back before any action, contain no expected result, execute unconditionally after close/EOF, and feed a separate stdin-only pure observer process. Action receipts remain opaque and unparsed until that observer is terminal; script review must verify imports, closure/global access, schedule, and dataflow.
- **Unowned marked child:** closed by controller-only direct spawn and pre-existing handle ownership. Pane requests after abort are rejected before creation.
- **Continuity policy:** closed. Real-home identity, exact path universe, full-tree/npm-sentinel distinctions, count/byte/file bounds, symlink/owner/special-file rules, canonical feature path/branch/SHA, clean-worktree receipts, nonmutating Git flags, index-drift checks, snapshot timing, and drift algebra are pinned.

## Survived counterexamples

1. **Server disappears after validation:** every released vector is top-level `-N`; no replacement server is permitted by the accepted exact-option premise. The action is primary FAIL, and changed/new generation or survivor makes containment/H5 fail.
2. **Action result tries to steer H2:** plans and helper order already exist before action, and the observer is a separate process with canonical stdin only. Any early parse, result-conditioned schedule/input, extra capability, or closure/global access is an exact-contract FAIL.
3. **Companion record never becomes valid:** the controller already owns its direct handle/new PGID and can terminate/reap it; invalid recording cannot satisfy primary H4 evidence.
4. **Late pane request races abort:** the create-only abort latch rejects it before controller spawn; the pane itself remains bounded by its record, fixed lifetime, exact server/pane anchors when available, and terminal audits.
5. **Server-B kill occurs in the forbidden phase:** it is explicitly between `DESCENDANTS_DRAINED` and `ACTION_FENCED`; only the latter starts the no-tmux phase.
6. **Fallback manufactures PASS:** primary evidence precedes containment; target-race action failure, malformed/missing oracle, observer violation, companion failure, survivor, or drift remains overall FAIL regardless of later cleanup.
7. **Builder varies protected scope:** exact roots, worktree identity, bounds, row schema, npm sentinel semantics, and snapshot points are normative; missing prerequisites are pre-spawn BLOCKED, not a builder choice.

## Nonclaims and next gate

This PASS does not approve execution, a replacement Plan, production lifecycle policy, lease/CAS/scheduler/schema behavior, PID-reuse-safe signaling, arbitrary descendant containment, auth, Claude, compaction, candidate freeze, or G0. V5's five audit limitations remain binding. The 800-line/resource bound is also binding: inability to implement R2 inside it is builder FAIL, not permission to omit a predicate.

The next eligible dependency is a clean-room builder task that authors but does not execute one immutable script. Exact script bytes then require fresh independent acceptance and skeptic PASS before any host run.
