---
type: Context Note
title: Final adversarial build identity QA orientation at d5d2f3f
actor: openai/qa
timestamp: '2026-07-31T23:05:48.912Z'
---
# Summary

Final QA target: exact reviewed commit d5d2f3f2dd37472f612e5b287f449a1c0b942285. No code or generated artifact edits are authorized.

Ultimate goal: make agentstate-lite reliable local-first shared memory whose executable identity and distribution projections are truthful, reproducible, and supportable.

Proximate goal: independently close the I1 QA gate by proving the complete runtime matrix and the marketplace transaction repair together, including package-level projections and ownership boundaries.

System model: one build identity owner receives explicit package, source, flavor, and contract facts; one registered executable entry supplies actual path and lazy SHA; all aliases, home, skill, MCP, and package receipts project from it. Marketplace regeneration samples commit and dirty exactly once before writes, passes the same snapshot through run, regenerateArtifacts, buildPluginBundle, and buildCliBundle, and reuses it across deterministic convergence proof. Bot outputs remain outside human PR ownership; the actor guard remains necessary because bot wrapper commit B cannot be known by the artifact built from source commit H.

Highest-risk checks: source entry must still hash src/index.ts; launch confidence must not exceed direct, PATH, npx, or layout evidence; dirty true must remain public truth; pass two must be a byte no-op with no second manifest bump; checker must sample before prepare; npm SKILL must be current while bot-owned plugin files remain unchanged.

Context read: current task, normative protocol section 1, Plan I1, executable-entry model and prior QA, marketplace regeneration system/analysis notes, changes-requested review 3579b98, and approved re-review d5d2f3f. Next: detached exact-SHA worktree, dependency install/root build, adversarial runtime matrix, focused CLI/script/package proofs, explicit ownership diff/status evidence, then exact-SHA PASS or FAIL note and sync.
