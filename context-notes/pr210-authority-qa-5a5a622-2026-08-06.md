---
type: Context Note
title: PR 210 supplementary installed-authority QA at 5a5a622
actor: codex-pr210-authority-qa
timestamp: '2026-08-06T19:52:18.838Z'
---
# Summary

PASS for supplementary installed-artifact authority and JSON/history QA of PR #210 at exact clean SHA `5a5a6229c840992e94cf26e91bd1f82b4bf18488`. No product counterexample was found. Actual packed-and-installed local-dev bytes refused six persistent mutation attempts before any target write, the supported layout installed and uninstalled both integrations correctly, and the corrected historical JSON control owned exactly the forms the repository has emitted.

## Goals

Ultimate goal: make agentstate-lite installable and self-orienting without claiming, rewriting, or deleting host configuration it did not generate.

Proximate goal: independently close the installed local-dev artifact authority/no-write and JSON/history positive evidence gaps at the exact reviewed SHA. This serves the ultimate goal by testing current physical provenance and lifecycle compatibility at the destructive boundary, without repeating the complete repository gate or the primary QA's 37-row host matrix.

## Existing authority and preflight controls

- Pure install-authority selection: PASS. Two selected test rows covered 12 explicit fixtures: one supported durable positive plus 11 fail-closed missing/transient/shadowed cases. Test runner result: 2/2 selected rows passed.
- Hook authority preflight no-write: PASS 1/1. A refused authority left the fresh base directory empty.
- Skill authority preflight no-write: PASS 1/1. A refused authority preserved the seeded Claude target byte/tree exactly and did not create the Codex target.

## Actual tarball/install matrix

The existing exact-SHA `local-dev` dist was packed stepwise with `npm pack --json --ignore-scripts`, producing `@holaxis/aslite@0.1.0-pre.3` with the expected 30-file payload. The tarball was then installed offline with `npm install --global --prefix ... --offline --ignore-scripts --no-audit --no-fund` into isolated prefixes.

Three distinct physical proof failures were invoked through the actually installed `dist/agentstate-lite.mjs`, for both `hook install --scope project --json` and `skill install --scope project --json`:

1. Wrong npm prefix: both commands exited 1 with structured `RUNTIME` refusal, reason `managed PATH bin is outside the npm global prefix bin directory`.
2. PATH shadows both managed aliases before the valid prefix: both commands exited 1 with structured `RUNTIME` refusal, reason `no managed PATH bin resolves to the running executable`.
3. Supported prefix/package/PATH shape but no stable `<prefix>/bin/node`: both commands exited 1 with structured `RUNTIME` refusal, reason `npm-prefix bin/node does not resolve to the running Node executable`.

Each command used its own fresh empty project. Before/after `find -mindepth 1` output was empty in all six cases: no Claude, Codex, OpenCode, skill, hook, or incidental project file was created.

A separate mismatched-node fixture was not counted: putting a foreign executable at `<prefix>/bin/node` also intercepted npm's `#!/usr/bin/env node` lookup, so `npm prefix --global` returned no prefix and the classifier correctly refused at an earlier proof leg. The missing-node fixture above isolates the stable-runtime leg without that environmental interaction.

## Supported positive and cleanup control

With an installed package at the scoped same-prefix layout, PATH resolving the package's managed aliases, and `<prefix>/bin/node` resolving to the running Node:

- `skill install --scope project --json`: exit 0, `changed:true`, both Claude and Codex skill manifests named `@holaxis/aslite@0.1.0-pre.3` with 26 managed files.
- `hook install --scope project --json`: exit 0. Claude and Codex persisted exactly `<prefix>/bin/node <prefix>/lib/node_modules/@holaxis/aslite/dist/agentstate-lite.mjs session-start`; OpenCode used the same launch projection.
- `hook uninstall` and `skill uninstall`: both exit 0 with `changed:true`. Both JSON hook command sets became empty; the generated OpenCode plugin and both managed skill directories were absent; a foreign sibling skill remained byte-identical as `foreign-preserve\n`.

## JSON/history positive block

The primary QA scratch file's intended assertions were: supported current/historical commands in both Claude and Codex JSON hosts must report owned/stale rather than unmanaged; status must not mutate bytes/mode; uninstall must remove every owned row while preserving a foreign sentinel.

Its proposed ten-command list contained one invalid positive, `npx -y agentstate-lite home`. The exact string does not appear in repository history, and the frozen classifier/tests enumerate the actual pre-session-start and session-start forms as `npx -y agentstate-lite` and `npx -y agentstate-lite session-start`. The built classifier therefore correctly preserved the `home` near-match as foreign. Treating it as owned would broaden destructive authority beyond a generated form.

The corrected 11-command current/history list (five canonical absolute layouts, four bare-bin forms, and the two actual legacy npx forms) was seeded into both JSON hosts with a foreign sentinel. Status exited 0 and reported `found 11 generated hook entries` for each host. Uninstall exited 0, removed all 11 rows from both hosts, preserved the sentinel exactly, and retained file mode `0640`. This materially closes the unrun JSON/history positive block while identifying the scratch expectation as a QA-fixture defect, not a product defect.

## Integrity and verdict

- Final HEAD: `5a5a6229c840992e94cf26e91bd1f82b4bf18488`.
- Final `git status --short`, `git diff --check`, `git diff --stat`, and `git diff --name-only`: clean/empty.
- Verdict: PASS; no P0/P1/P2 finding and no authority/no-write counterexample.
- Next gate: orchestration may combine this receipt with the independent exact-SHA review, primary QA, supplementary OpenCode QA, and repository/GitHub Node 20/22/26 results. This note does not authorize merge by itself.

[tracks](../tasks/hook-compatibility-ownership.md)

[review input](pr210-exact-review-5a5a622-2026-08-06.md)

[primary QA](pr210-adversarial-qa-5a5a622-2026-08-06.md)

[OpenCode QA](pr210-opencode-qa-5a5a622-2026-08-06.md)
