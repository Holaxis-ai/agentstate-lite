---
type: Context Note
title: Revision 3 R6 host-probe reorientation after context boundary
actor: codex-precompact-v3-orchestrator
timestamp: '2026-08-04T01:22:55.729Z'
---
# Summary

Revision 3 remains blocked before implementation: a repaired, bounded H1-H5 host probe is still being compressed and has neither frozen nor run. The exact-byte dual static review, one no-auth execution, independent evidence audit, and exact Plan dual review remain mandatory in that order.

# Purpose

Ultimate goal: agentstate-lite is shared, versioned, conflict-safe memory for concurrent agent fleets, in plain text and owned by the user.

Proximate goal: produce, dual-review, run, and independently audit a bounded exact-host H1-H5 probe, then draft and dual-review the repaired revision-3 Plan; this serves the ultimate goal by validating the installed-host lifecycle boundary without creating a second acceptance authority.

# Current system model

Revision 3 is blocked before implementation. Accepted T0-T4 evidence establishes the exact-full-identity direction and the installed Claude lifecycle ordering, with SessionStart(source=compact) as the load-bearing restore rail. R5 of the T3.5 candidate/acceptance Plan was product-PASS but skeptic-FAIL because cleanup controls could become unmodeled principals and exact PID proof was demanded for short-lived hooks the host does not expose reliably.

The R6 repair boundary is intentionally split. A scratch probe may answer only five host questions (H1-H5): closed no-auth tmux control, discard result observability, two-requester owner/successor feasibility, declared marked-detacher descendant-first cleanup, and terminal host/protected-state continuity. It must not implement or claim lifecycle policy, CAS, lease, scheduler, schema registry, verdict authority, Claude, or authentication. Those policy claims belong later in the one executable lifecycle authority and its fake-scheduler tests.

The first clean-room host-probe script was never executed and received two exact static FAIL verdicts. Its seven blocker classes were: pre-registration gaps; Node-to-tmux exec identity drift; incomplete group/client teardown; potentially mutating Git observation; non-structural H2 receipt isolation; missing simultaneous H3 requester evidence; and fail-open terminal/receipt/digest binding.

# Live phase state

Builder `/root/v3_r6_host_probe_builder2` is repairing those seven classes in `/private/tmp/aslite-t35-r6-host-probe.v2` under a strict no-execution contract. At the latest checkpoint all seven repair classes were represented, but the draft remained 881 effective LOC, owner-writable, unfrozen, and therefore ineligible for review. The builder is compressing without weakening the contract and must return FAIL if it cannot reach the 800-line ceiling. No host-probe run root exists from this R6 attempt. The previous rejected R1 bytes and reviews remain evidence and must not be overwritten or treated as executable acceptance.

When the builder freezes a new digest, the orchestrator must independently verify hashes, modes, effective LOC, and syntax; then the product acceptance reviewer and structurally isolated skeptic must each review the same exact bytes without executing them. Only dual static PASS opens one exact no-auth H1-H5 run. Exact retained evidence then requires independent audit before any replacement Plan is authored. Exact Plan dual PASS is required before tests or production code.

# Unverified assumptions and hard stops

- The repaired script may not fit under 800 effective LOC; if not, return FAIL rather than weakening evidence.
- Transition-aware cleanup and all-client absence have not yet passed static review.
- The H1-H5 probe has not run; no host fact from it is accepted yet.
- The repaired R6 Plan does not exist; R5 remains rejected.
- No implementation authorization has opened. Review must precede QA for every later exact code SHA.

# Reloaded guidance

At this context boundary the orchestrator re-read `CLAUDE.md` and reloaded `holaxis-self-awareness`, `holaxis-cognitive-ecosystem`, `agentstate-lite`, and `holaxis-orchestrator`. The deprecated fixed `context-notes/pre-compact-main` was not overwritten; this scoped note records the current revision-3 state.
