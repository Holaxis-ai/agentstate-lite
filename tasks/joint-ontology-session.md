---
type: Task
title: Founders' joint ontology session (gates typed-relationships rung c)
status: todo
priority: '2'
description: >-
  Founders' session to adjudicate plans/relationship-ontology (the distilled
  proposal — READ THAT FIRST; it carries six numbered asks). This task is the
  scheduling gate for typed-relationships rung c. The full deliberation history
  lives in research/ontology-deliberation; the audited label mechanics in
  docs/relationships. Session output: decisions/ docs per ask + filed tasks for
  the NOW-horizon items.
actor: brian-claude
timestamp: '2026-07-14T21:59:02.498Z'
---
## What this is

A working session for Brian + Mike to adjudicate the relationship-ontology proposal.
Scheduling is the open work; the thinking is done and distilled.

## Read this first (5 minutes)

[The proposal](../plans/relationship-ontology.md) — plans/relationship-ontology. It is
self-contained: product framing, a five-class kernel with one-line behaviors, the
human-decides/agent-implements division of labor, a three-horizon build list, explicit
non-goals, and SIX NUMBERED ASKS to ratify or amend. That document IS the agenda.

## What the session decides

Work through the proposal's six asks in order. The only questions it deliberately
leaves open for the room:

1. The grounding/specification naming call (one class with a wide tent, or two names
   sharing one consumer so "implements" has an obvious home).
2. Anything Mike challenges — every claim in the proposal traces to evidence (below),
   so disagreements can go straight to the source.
3. Deferred by prior agreement, only if time remains: the lite<->Holaxis vocabulary
   alignment (nothing in lite blocks on it).

## Done when

Each ratified/amended ask lands as a decisions/ doc linked from
roadmap-items/typed-relationships; the NOW-horizon items become filed tasks; rung c
on the roadmap item is consequently resolved (buildable tasks or retired with
reasoning).

## Evidence trail (read only what you want to challenge)

- [Relationships index](../docs/relationships.md) — the audited mechanism: three
  tiers, every label and what actually consumes it, the depends-on-is-inert finding.
- [Deliberation log](../research/ontology-deliberation.md) — the complete history:
  the original six questions, two frame versions, five adversarial reviewer reports
  (two skeptics, a mechanism review, an agent-experience review, an ontology
  researcher), the corpus findings (284 edges; the id-default discovery), and
  Decision Zero with Brian's demand-enabling resolution. Kept for provenance; not
  required reading.
