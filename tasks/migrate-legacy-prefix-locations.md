---
type: Task
title: >-
  OPEN DECISION: move legacy pages-*/ files to views-*/ folders (the address
  dial)
status: todo
priority: '4'
assignee: ''
description: >-
  The third dial of the legacy migration (Brian, 2026-07-23: 'possible, not
  preferred' — deliberately left open, NOT bundled with the name migration). A
  doc's folder+name is its IDENTITY: moving means create-at-new-address +
  delete-old + rewrite every inbound link + update each registration's entry
  pairing; bookmarks/deep links to old addresses die. Measured stock: 8 registry
  docs + 8 blobs, ~2 real inbound links — small in absolute terms, categorically
  harder than in-file renames.

  CASE FOR: one canonical location; ids stop showing pages-registry/ in listings
  ('why is this one under pages?').

  CASE AGAINST: addresses are expected to be historical; recognition of old
  prefixes costs two grammar constants; every identity change breaks something
  someone saved.

  IF DONE: prefix-recognition removal can follow as its own removal step. IF
  DECLINED: record the decision here and the prefixes stay recognized
  permanently — a deliberate lumper choice, not decay.

  Depends on Phase 2a (content first, so a move — if ever — moves already-clean
  files).
actor: claude-main-viewauthoring
timestamp: '2026-07-23T23:48:25.987Z'
---
[depends on](migrate-legacy-page-bridge-stock.md)
