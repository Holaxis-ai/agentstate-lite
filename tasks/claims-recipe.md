---
type: Task
title: >-
  Claims-as-a-recipe: the falsifiable test of the composition thesis (port
  holaxis-claims)
status: done
priority: '1'
description: >-
  VERDICT: WIN (2026-07-06 evening, one session from design sign-off to locked
  claim). The composition thesis holds: holaxis-claims ported as ~2 markdown
  documents (recipe.md + conventions/claim.md at
  ~/projects/Holaxis/recipes/claims — the FIRST external RecipeSource consumer,
  own git repo), zero engine changes, zero scaffold code. ALL FIVE CRITERIA
  DEMONSTRATED LIVE on the claims/repo-stats testbed: (1) write-time validation
  — illegal status rejected at write with a teaching envelope; (2) queryability
  — list --prefix claims/ + kind-aware projection hints; (3) concurrent-agent
  safety — deliberate two-writer race: stale token refused with STALE_HEAD +
  re-read guidance (the ported --parent-event-id semantics, engine-grade); (4)
  one implementation — recipe add replaced setup_claims.py scaffolding entirely;
  (5) citation links — docs/core now LINKS the test-count claim, backlinks
  enumerate citing docs (the capability JSONL structurally cannot have). BONUS:
  first-evening drift catch — docs/core says '750+' tests, the LOCKED claim
  (challenged, independently re-verified, locked by CAS dance) says 759. Five
  claims filed with full provenance (evidence_command + evidence_commit
  1463bbd): test-count(locked), engine-loc, cli-loc, ui-embed-size,
  backend-count (active, awaiting challenge cycles). FOLLOW-UPS: refresh
  docs/core + README numbers from locked claims (small); transition-rule
  enforcement (locked->active illegal) NOT felt in one cycle — stays a
  watch-item, not a build; consider publishing the claims recipe as the public
  example recipe (human decision). CONSEQUENCE FOR CORE.md: the 'deeper claim'
  passed its falsifiable test — recipes graduate from bet toward product
  mechanism; cookbook design (typed link glue) is now worth its design session.
timestamp: '2026-07-06T20:24:41.742Z'
---
[plans/claims-recipe](../plans/claims-recipe.md)
