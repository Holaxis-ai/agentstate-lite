---
type: Task
title: 'Git sharing tier: bundle-in-a-git-repo as the OSS multi-human floor'
status: in_progress
priority: '1'
description: >-
  TIER 0 SHIPPED (2026-07-06 evening): the board LIVES IN GIT — migrated via
  byte channel (50 docs + blobs, verified), local bundle at ../agentstate-board,
  binding resolves it, bare commands are the proof, worker frozen with banner.
  HARVESTED FRICTION → FIXES: (1) sync verb — spec = the migration script,
  QUEUED (design decision: reserved wire-sync verb vs git-tier
  commit/pull-rebase/push semantics); (2) field-level reads — SHIPPED same day
  ('doc read --field', headline case head_version→CAS token, plugin v1.0.2; the
  grep/sed dance is dead); (3) body-edit guidance — SHIPPED in SKILL.md same
  commit. REMAINING Tier 1: the sync verb; GitHub private remote for the board
  (Mike's call).
timestamp: '2026-07-07T18:02:14.365Z'
---
[design](../plans/sync-verb.md)
